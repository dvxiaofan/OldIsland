<!--components/navig/index.wxml-->

<view class="container">
	<image bind:tap="onLeft" class="navig-icon" src="{{latest ? disLeftSrc : leftSrc}}" />
	<text class="navig-title">{{title}}</text>
	<image bind:tap="onRight" class="navig-icon" src="{{first ? disRightSrc : rightSrc}}" />
</view>
